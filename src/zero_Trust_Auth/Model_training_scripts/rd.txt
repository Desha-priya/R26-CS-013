
###train_models.py 
    -> Trains Isolation Forest + One-Class SVM on combined behavioral profiles
			# Saves: models/isolation_forest.pkl, models/oneclass_svm.pkl, models/scaler.pkl
			#        models/user_profiles.pkl  (per-user feature vectors for live comparison)
-------------------------
###risk_engine.py 
    - > The novelty. 
    A single Python class that takes a model score + current context mode + any cross-layer alerts and outputs a final risk score 0.0–1.0 with an adaptive threshold.

  
  4 tests prove:

Test 1→3: Same user, same behaviour, threshold drops from 0.65 → 0.40 → 0.30 as context tightens. 
            That's the novelty proven.
Test 4: Random attacker features → 100% risk, step_up_auth instantly. That's about detection working.